meta:
  environment: python
  toolchain:
    name: pip
    version: 3.10
build:
  requirementsPath: backend/requirements.txt
run:
  scriptName: backend/main.py
  persistenceMount: /data
  containerPort: 80
